# Hosts like Render set NODE_ENV=production, which makes `npm install` skip
# devDependencies. The build needs them (TypeScript, @types/*, tsc-alias,
# prisma), so force dev deps to install everywhere. Runtime still uses only
# the compiled dist/ + production deps.
include=dev
